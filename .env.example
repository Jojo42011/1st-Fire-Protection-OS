# ─────────────────────────────────────────────────────────────
# 1st FP Operating System — environment
# Every feature no-ops cleanly without its key. A keyless boot still runs
# on seeded sample data. Add keys to light up each capability.
# ─────────────────────────────────────────────────────────────

# Core server
PORT=3900
DB_PATH=./data/1stfp.db

# ── AI & Reasoning (the brain) ── add either one to enable live conversation/drafting
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-4-8
ANTHROPIC_FAST_MODEL=claude-haiku-4-5-20251001
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBED_MODEL=text-embedding-3-small   # embeddings power hybrid memory recall

# ── Voice & Telephony (Call Receptionist goes live when present) ──
# Live path: forward 210-377-FIRE -> Twilio -> Vapi (BYO OpenAI key) -> transfer into Teams
VAPI_API_KEY=            # also enables /api/calls/sync backfill from Vapi's REST API
VAPI_SERVER_SECRET=      # shared secret; must match Vapi's Server URL secret (x-vapi-secret header)
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# ── A2P 10DLC registration (SMS) ── operator-only facts for the Twilio Brand/Campaign.
# Everything else is pre-filled from the founder layer (server/src/config/tenDLC.ts);
# these are the values only the business owns. See docs/10dlc-twilio.md.
TWILIO_A2P_EIN=              # 9-digit IRS EIN / Tax ID, no dash (REQUIRED to submit)
TWILIO_A2P_DUNS=            # D-U-N-S number (optional, speeds Standard-brand vetting)
TWILIO_A2P_CONTACT_FIRST=  # authorized contact first name (defaults to Chris)
TWILIO_A2P_CONTACT_LAST=   # authorized contact last name (defaults to Holcomb)
TWILIO_A2P_CONTACT_TITLE=  # e.g. Chief Operating Officer
TWILIO_A2P_CONTACT_EMAIL=  # defaults to dispatch@1stfpcompanies.com
TWILIO_A2P_CONTACT_PHONE=  # E.164, defaults to +12103773473

# ── Field service system of record (jobs, invoices, completions) ──
SERVICETRADE_TOKEN=

# ── Comms & Ops ──
MS365_TOKEN=            # Microsoft 365 / Teams (call transfers, email)
N8N_API_KEY=           # workflow orchestration (call logging, review requests)

# ── Payments & Finance (Invoice Collector) ──
STRIPE_API_KEY=

# ── Reputation (Review Collector) ──
GOOGLE_BUSINESS_TOKEN=
FACEBOOK_PAGE_TOKEN=

# ── Messaging ──
GMAIL_ACCESS_TOKEN=
